**FundPulse v2.0.1 产品重构设计文档**
_Theme: Cyber-Fintech / 赛博金融纪_

---

## 1. 设计系统 (Design System)

### 1.1 色彩架构：深空霓虹 (Deep Space Neon)

**css**复制

```css
:root {
  /* 背景层级 */
  --bg-void: #030305; /* 主背景 - 绝对黑 */
  --bg-surface: rgba(15, 15, 25, 0.6); /* 玻璃态卡片 */
  --bg-elevated: rgba(25, 25, 40, 0.8); /* 悬浮层 */

  /* 核心霓虹色 */
  --neon-red: #ff2d55; /* 主强调 - 电光红 */
  --neon-red-glow: rgba(255, 45, 85, 0.4);
  --neon-blue: #00d4ff; /* 科技蓝 - 数据流动 */
  --neon-blue-glow: rgba(0, 212, 255, 0.3);
  --neon-purple: #bf5af2; /* AI功能色 - 紫罗兰 */
  --neon-purple-glow: rgba(191, 90, 242, 0.4);
  --neon-amber: #ff9f0a; /* 预警 - 琥珀 */

  /* 金融语义 (高对比) */
  --up: #ff453a; /* 涨 - 鲜红霓虹 */
  --down: #32d74b; /* 跌 - 翠绿霓虹 */
  --neutral: #8e8e93; /* 中性 - 银灰 */

  /* 文字层级 */
  --text-primary: rgba(255, 255, 255, 0.95);
  --text-secondary: rgba(255, 255, 255, 0.6);
  --text-tertiary: rgba(255, 255, 255, 0.35);

  /* 边框与分隔 */
  --border-subtle: rgba(255, 255, 255, 0.08);
  --border-glow: rgba(255, 45, 85, 0.3);
  --grid-line: rgba(255, 255, 255, 0.03);
}
```

### 1.2 字体系统

**css**复制

```css
--font-display: "Space Grotesk", "Inter", sans-serif;
--font-mono: "JetBrains Mono", "SF Mono", monospace;
--font-body: -apple-system, BlinkMacSystemFont, "Segoe UI", sans-serif;
```

### 1.3 玻璃拟态组件 (Glassmorphism)

**css**复制

```css
.glass-card {
  background: linear-gradient(
    135deg,
    rgba(255, 255, 255, 0.03) 0%,
    rgba(255, 255, 255, 0.01) 100%
  );
  backdrop-filter: blur(20px) saturate(180%);
  -webkit-backdrop-filter: blur(20px) saturate(180%);
  border: 1px solid var(--border-subtle);
  border-radius: 16px;
  box-shadow: 0 8px 32px rgba(0, 0, 0, 0.4), inset 0 1px 0 rgba(255, 255, 255, 0.05);
  transition: all 0.3s cubic-bezier(0.4, 0, 0.2, 1);
}

.glass-card:hover {
  border-color: var(--neon-red);
  box-shadow: 0 12px 40px rgba(0, 0, 0, 0.5), 0 0 20px var(--neon-red-glow),
    inset 0 1px 0 rgba(255, 255, 255, 0.1);
  transform: translateY(-2px);
}
```

### 1.4 数字翻牌动画系统 (Flip Animation)

**数字翻牌 React 组件最好帮忙封装成一个公共组件**

**css**复制

```css
/* 基础翻牌容器 */
.flip-number-container {
  display: inline-flex;
  perspective: 1000px;
  font-variant-numeric: tabular-nums;
  font-family: var(--font-mono);
}

/* 单个数字位 */
.flip-digit {
  position: relative;
  width: 0.6em;
  height: 1.2em;
  line-height: 1.2em;
  text-align: center;
  overflow: hidden;
}

/* 翻牌动画关键帧 */
@keyframes flip-up {
  0% {
    transform: rotateX(0deg);
    opacity: 1;
  }
  50% {
    transform: rotateX(-90deg);
    opacity: 0.3;
  }
  100% {
    transform: rotateX(-180deg);
    opacity: 1;
  }
}

@keyframes flip-down {
  0% {
    transform: rotateX(0deg);
    opacity: 1;
  }
  50% {
    transform: rotateX(90deg);
    opacity: 0.3;
  }
  100% {
    transform: rotateX(180deg);
    opacity: 1;
  }
}

/* 涨跌颜色渐变动画 */
@keyframes glow-pulse-red {
  0%,
  100% {
    text-shadow: 0 0 5px rgba(255, 69, 58, 0.5);
    color: #ff453a;
  }
  50% {
    text-shadow: 0 0 20px rgba(255, 69, 58, 0.9), 0 0 40px rgba(255, 69, 58, 0.4);
    color: #ff6b6b;
  }
}

@keyframes glow-pulse-green {
  0%,
  100% {
    text-shadow: 0 0 5px rgba(50, 215, 75, 0.5);
    color: #32d74b;
  }
  50% {
    text-shadow: 0 0 20px rgba(50, 215, 75, 0.9), 0 0 40px rgba(50, 215, 75, 0.4);
    color: #4ade80;
  }
}

/* 数字更新时的翻牌效果类 */
.number-flip-up {
  animation: flip-up 0.3s cubic-bezier(0.4, 0, 0.2, 1);
}

.number-flip-down {
  animation: flip-down 0.3s cubic-bezier(0.4, 0, 0.2, 1);
}

/* 涨跌状态样式 */
.trend-up {
  animation: glow-pulse-red 2s ease-in-out infinite;
  color: var(--up);
}

.trend-down {
  animation: glow-pulse-green 2s ease-in-out infinite;
  color: var(--down);
}

/* 背景扫描线 */
.bg-scanline {
  background-image: linear-gradient(var(--grid-line) 1px, transparent 1px),
    linear-gradient(90deg, var(--grid-line) 1px, transparent 1px);
  background-size: 50px 50px;
}
```

---

## 2. 信息架构 (IA)

### 2.1 导航结构

复制

```
FundPulse 2.0.1
├── 首页 (Home)
│   ├── Hero Section
│   ├── 横向滚动指数条 (IndexMarquee)
│   ├── 热门板块榜 (SectorBoard)
│   └── 基金排行榜 (FundRanking)
│       └── 弹窗: 基金详情 (含AI诊断Tab)
└── 自选 (Portfolio)
    ├── 资产概览卡片
    ├── 基金列表 (详细版)
    └── AI诊断抽屉 (单基金/多基金对比)
```

---

## 3. 首页重构 (Home v2.0.1)

### 3.1 横向指数滚动条 (IndexMarquee)

**数据源** : 腾讯财经 `qt.gtimg.cn` (JSONP)
**特性** : 自动循环滚动、数字翻牌动画、触摸滑动支持

**tsx**复制

```tsx
// components/IndexMarquee.tsx
interface IndexData {
  name: string;
  code: string;
  price: number;
  change: number;
  changePercent: number;
  volume?: string;
}

// 字段映射 (腾讯API)
// 0:市场 1:名称 2:代码 3:最新价 4:昨收 5:今开
// 29:涨跌额 30:涨跌幅% 31:最高 32:最低 35:成交额

const parseTencentIndex = (text: string): IndexData[] => {
  const lines = text.split(";").filter((l) => l.includes("="));
  return lines.map((line) => {
    const [_, dataStr] = line.split("=");
    const d = dataStr.replace(/^"|"$/g, "").split("~");
    return {
      name: d[1],
      code: d[2],
      price: parseFloat(d[3]),
      change: parseFloat(
        d[29] || (parseFloat(d[3]) - parseFloat(d[4])).toString()
      ),
      changePercent: parseFloat(d[30] || "0"),
    };
  });
};

export const IndexMarquee = () => {
  const [indices, setIndices] = useState<IndexData[]>([]);
  const containerRef = useRef<HTMLDivElement>(null);
  const [isDragging, setIsDragging] = useState(false);

  // 获取数据
  useEffect(() => {
    const load = async () => {
      const codes =
        "sh000001,sz399001,sz399006,sh000300,hkHSI,hkHSCEI,usIXIC,usDJI";
      // JSONP 加载...
      const data = await fetchViaJSONP(
        `https://qt.gtimg.cn/q=${codes}`,
        parseTencentIndex
      );
      setIndices(data);
    };
    load();
    const timer = setInterval(load, 30000); // 30秒刷新
    return () => clearInterval(timer);
  }, []);

  // 自动滚动动画
  useEffect(() => {
    const container = containerRef.current;
    if (!container || indices.length === 0) return;

    let animationId: number;
    const scroll = () => {
      if (!isDragging && container) {
        container.scrollLeft += 0.8;
        if (container.scrollLeft >= container.scrollWidth / 2) {
          container.scrollLeft = 0; // 无缝循环
        }
      }
      animationId = requestAnimationFrame(scroll);
    };
    animationId = requestAnimationFrame(scroll);
    return () => cancelAnimationFrame(animationId);
  }, [indices, isDragging]);

  // 数字翻牌组件
  const FlipNumber = ({
    value,
    decimals = 2,
  }: {
    value: number;
    decimals?: number;
  }) => {
    const [displayValue, setDisplayValue] = useState(value);
    const [isFlipping, setIsFlipping] = useState(false);

    useEffect(() => {
      if (value !== displayValue) {
        setIsFlipping(true);
        setTimeout(() => {
          setDisplayValue(value);
          setIsFlipping(false);
        }, 150);
      }
    }, [value]);

    return (
      <span
        className={`inline-block tabular-nums ${
          isFlipping ? "number-flip-down" : ""
        }`}
      >
        {displayValue.toFixed(decimals)}
      </span>
    );
  };

  return (
    <div className="relative w-full bg-surface/50 border-y border-white/5 overflow-hidden">
      {/* 左右渐变遮罩 */}
      <div className="absolute left-0 top-0 bottom-0 w-20 bg-gradient-to-r from-void to-transparent z-10 pointer-events-none" />
      <div className="absolute right-0 top-0 bottom-0 w-20 bg-gradient-to-l from-void to-transparent z-10 pointer-events-none" />

      <div
        ref={containerRef}
        className="flex gap-4 px-6 py-4 overflow-x-auto scrollbar-hide cursor-grab active:cursor-grabbing"
        onMouseDown={(e) => {
          setIsDragging(true); /* 记录startX */
        }}
        onMouseUp={() => setIsDragging(false)}
        onMouseLeave={() => setIsDragging(false)}
      >
        {/* 双份数据实现无缝循环 */}
        {[...indices, ...indices].map((idx, i) => (
          <div
            key={`${idx.code}-${i}`}
            className="relative flex-shrink-0 w-[220px] p-4 rounded-xl bg-white/5 border border-white/10 backdrop-blur-sm group hover:border-white/20 transition-all"
          >
            {/* 涨跌渐变背景 */}
            {idx.change > 0 && (
              <div className="absolute inset-0 bg-gradient-to-r from-red-500/0 via-red-500/5 to-red-500/0 animate-pulse" />
            )}
            {idx.change < 0 && (
              <div className="absolute inset-0 bg-gradient-to-r from-green-500/0 via-green-500/5 to-green-500/0 animate-pulse" />
            )}

            <div className="relative z-10">
              <div className="flex items-center justify-between mb-2">
                <span className="text-sm text-text-secondary">{idx.name}</span>
                <span
                  className={`text-xs px-2 py-0.5 rounded-full font-medium ${
                    idx.change >= 0
                      ? "bg-red-500/20 text-red-400"
                      : "bg-green-500/20 text-green-400"
                  }`}
                >
                  {idx.change >= 0 ? "+" : ""}
                  {idx.changePercent.toFixed(2)}%
                </span>
              </div>

              <div className="text-3xl font-mono font-bold text-text-primary tracking-tighter">
                <FlipNumber value={idx.price} />
              </div>

              <div
                className={`text-sm font-mono mt-1 flex items-center gap-1 ${
                  idx.change >= 0 ? "trend-up" : "trend-down"
                }`}
              >
                {idx.change >= 0 ? "↑" : "↓"}
                <FlipNumber value={Math.abs(idx.change)} decimals={2} />
              </div>
            </div>
          </div>
        ))}
      </div>
    </div>
  );
};
```

### 3.2 热门板块榜 (SectorBoard)

**数据源** : 东方财富 `push2.eastmoney.com/api/qt/clist/get` (fs=m:90+t:2)
**特性** : 涨幅/跌幅双榜切换、涨跌家数可视化、领涨股 Hover 显示

**tsx**复制

```tsx
// components/SectorBoard.tsx
interface Sector {
  code: string;
  name: string;
  changePercent: number;
  upCount: number;
  downCount: number;
  leadingStock?: {
    name: string;
    changePercent: number;
  };
}

export const SectorBoard = () => {
  const [sectors, setSectors] = useState<Sector[]>([]);
  const [activeTab, setActiveTab] = useState<"up" | "down">("up");

  useEffect(() => {
    fetchSectors();
  }, [activeTab]);

  const fetchSectors = async () => {
    // fs=m:90+t:2 (行业板块)
    const url =
      "https://push2.eastmoney.com/api/qt/clist/get?np=1&fltt=2&fs=m:90+t:2+f:!50&fields=f12,f14,f3,f104,f105,f128,f141&fid=f3&pn=1&pz=20";

    const data = await fetchViaJSONP(url, (res) => {
      return res.data?.diff?.map((item: any) => ({
        code: item.f12,
        name: item.f14.replace("行业", ""),
        changePercent: item.f3,
        upCount: item.f104,
        downCount: item.f105,
        leadingStock: item.f128
          ? { name: item.f128, changePercent: item.f141 }
          : null,
      }));
    });

    // 根据Tab筛选
    if (activeTab === "up") {
      setSectors(data.filter((s: Sector) => s.changePercent > 0).slice(0, 10));
    } else {
      setSectors(
        data
          .filter((s: Sector) => s.changePercent < 0)
          .sort((a: Sector, b: Sector) => a.changePercent - b.changePercent)
          .slice(0, 10)
      );
    }
  };

  return (
    <div className="px-6 py-4">
      <div className="glass-card p-5">
        <div className="flex items-center justify-between mb-5">
          <div className="flex items-center gap-2">
            <i className="ri-bar-chart-grouped-line text-neon-blue text-xl" />
            <h3 className="font-display font-bold text-lg">热门板块</h3>
          </div>

          <div className="flex bg-black/30 rounded-lg p-1">
            <button
              onClick={() => setActiveTab("up")}
              className={`px-4 py-1.5 rounded-md text-sm font-medium transition-all ${
                activeTab === "up"
                  ? "bg-red-500/20 text-red-400 shadow-[0_0_15px_rgba(255,68,58,0.3)]"
                  : "text-text-secondary"
              }`}
            >
              🔥 涨幅榜
            </button>
            <button
              onClick={() => setActiveTab("down")}
              className={`px-4 py-1.5 rounded-md text-sm font-medium transition-all ${
                activeTab === "down"
                  ? "bg-green-500/20 text-green-400 shadow-[0_0_15px_rgba(50,215,75,0.3)]"
                  : "text-text-secondary"
              }`}
            >
              ❄️ 跌幅榜
            </button>
          </div>
        </div>

        <div className="grid grid-cols-2 md:grid-cols-5 gap-3">
          {sectors.map((sector, idx) => (
            <motion.div
              key={sector.code}
              initial={{ opacity: 0, scale: 0.95 }}
              animate={{ opacity: 1, scale: 1 }}
              transition={{ delay: idx * 0.05 }}
              className={`relative p-4 rounded-xl border backdrop-blur-sm cursor-pointer overflow-hidden group ${
                activeTab === "up"
                  ? "bg-red-500/5 border-red-500/20 hover:bg-red-500/10"
                  : "bg-green-500/5 border-green-500/20 hover:bg-green-500/10"
              }`}
            >
              {/* 热度条 */}
              <div
                className={`absolute bottom-0 left-0 h-0.5 ${
                  activeTab === "up" ? "bg-red-500" : "bg-green-500"
                }`}
                style={{
                  width: `${Math.min(
                    Math.abs(sector.changePercent) * 8,
                    100
                  )}%`,
                }}
              />

              <div className="flex items-center justify-between mb-1">
                <span className="text-xs text-text-tertiary">#{idx + 1}</span>
                <span
                  className={`text-lg font-mono font-bold ${
                    activeTab === "up" ? "text-red-400" : "text-green-400"
                  }`}
                >
                  {sector.changePercent > 0 ? "+" : ""}
                  {sector.changePercent.toFixed(2)}%
                </span>
              </div>

              <h4 className="font-medium text-text-primary mb-3 truncate">
                {sector.name}
              </h4>

              <div className="flex items-center justify-between text-xs text-text-secondary">
                <span className="flex items-center gap-1 text-red-400">
                  <i className="ri-arrow-up-line" />
                  {sector.upCount}
                </span>
                <span className="w-px h-3 bg-white/10" />
                <span className="flex items-center gap-1 text-green-400">
                  <i className="ri-arrow-down-line" />
                  {sector.downCount}
                </span>
              </div>

              {/* Hover显示领涨股 */}
              <div className="mt-2 pt-2 border-t border-white/5 opacity-0 group-hover:opacity-100 transition-all text-xs">
                <span className="text-text-tertiary">领涨: </span>
                <span className="text-text-primary">
                  {sector.leadingStock?.name || "-"}
                </span>
                {sector.leadingStock && (
                  <span
                    className={
                      sector.leadingStock.changePercent >= 0
                        ? "text-red-400 ml-1"
                        : "text-green-400 ml-1"
                    }
                  >
                    {sector.leadingStock.changePercent > 0 ? "+" : ""}
                    {sector.leadingStock.changePercent.toFixed(1)}%
                  </span>
                )}
              </div>
            </motion.div>
          ))}
        </div>
      </div>
    </div>
  );
};
```

### 3.3 基金排行榜 (FundRanking)

**数据源** : 天天基金 `fund.eastmoney.com/data/rankhandler.aspx` (JSONP)
**部署注意** : GitHub Pages 需通过 Cloudflare Worker 代理

**tsx**复制

```tsx
// 简化的基金排行组件核心逻辑
const fetchRanking = async () => {
  const isDev = import.meta.env.DEV;
  const url = isDev
    ? `http://fund.eastmoney.com/data/rankhandler.aspx?op=ph&dt=kf&ft=all&sc=1nzf&st=desc&pn=50&_=${Date.now()}`
    : `https://your-worker.workers.dev?op=ph&dt=kf&ft=all&sc=1nzf&st=desc&pn=50`;

  // JSONP解析...
};
```

---

## 4. 自选页面 (Portfolio)

### 4.1 资产概览卡片

**tsx**复制

```tsx
<DashboardGrid className="grid grid-cols-1 md:grid-cols-3 gap-4 p-6">
  {/* 总资产 / 今日盈亏 / AI健康分 */}
  {/* 参考之前提供的Dashboard代码 */}
</DashboardGrid>
```

### 4.2 AI 诊基系统 (v2.0)

**流程** : 基金选择器(1-3 只) → 分析模式(单基金/对比) → 流式结果展示

**tsx**复制

```tsx
// AI诊断状态管理
interface AIState {
  selectedFunds: Fund[];
  mode: "single" | "compare" | null;
  isAnalyzing: boolean;
  result: AIResult | null;
}

// 基金选择器限制
const MAX_SELECT = 3;
const toggleSelection = (fund: Fund) => {
  if (selected.find((s) => s.code === fund.code)) {
    setSelected(selected.filter((s) => s.code !== fund.code));
  } else if (selected.length < MAX_SELECT) {
    setSelected([...selected, fund]);
  }
};
```

**Prompt 策略** :

- **单基金** : 深度诊断(技术面/基本面/风险评估/操作建议)
- **对比模式** : 互补性分析/配置建议/最终推荐

---

## 5. 数据源汇总

**表格**复制

| 功能     | 接口                                         | 方式    | 部署注意              |
| :------- | :------------------------------------------- | :------ | :-------------------- |
| 指数行情 | `qt.gtimg.cn`                                | JSONP   | 直接调用              |
| 板块排行 | `push2.eastmoney.com`                        | JSONP   | 直接调用              |
| 基金排行 | `fund.eastmoney.com/rankhandler.aspx`        | JSONP   | 需 Worker 代理(HTTPS) |
| 基金持仓 | `fund.eastmoney.com/pingzhongdata/{code}.js` | JS 加载 | 直接调用              |
| 指数详细 | `push2.eastmoney.com/api/qt/stock/get`       | JSONP   | 直接调用              |

---

## 6. 部署检查清单

- [ ] 数字翻牌 CSS Keyframes 已添加到全局样式
- [ ] Cloudflare Worker 已部署用于基金排行代理
- [ ] 首页三栏布局(指数/板块/基金)已验证移动端适配
- [ ] AI 诊基选择器限制 3 只基金逻辑已测试
- [ ] 所有 JSONP 调用在 HTTPS 环境下的降级方案已准备
