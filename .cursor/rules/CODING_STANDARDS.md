# FundPulse 代码规范与技术标准

本文档定义了 FundPulse 项目的代码规范、最佳实践和技术标准。所有代码必须严格遵循这些规范。

## 📋 目录

- [代码质量要求](#代码质量要求)
- [代码检查与 Lint](#代码检查与-lint)
- [代码整洁原则](#代码整洁原则)
- [设计模式应用](#设计模式应用)
- [架构设计原则](#架构设计原则)
- [React/TypeScript 规范](#reacttypescript-规范)
- [API 与数据管理](#api-与数据管理)
- [错误处理](#错误处理)
- [性能优化](#性能优化)

---

## 代码质量要求

### 1. 必须遵循的原则

- ✅ **零容忍未使用代码**：所有变量、函数、导入必须被使用，否则删除
- ✅ **类型安全**：充分利用 TypeScript 类型系统，避免使用 `any`
- ✅ **代码可读性**：代码应该自解释，注释只用于解释"为什么"而非"是什么"
- ✅ **单一职责**：每个函数、组件、模块只做一件事
- ✅ **DRY 原则**：不要重复代码，提取公共逻辑

### 2. 代码提交前检查清单

在提交代码前，**必须**完成以下检查：

```bash
# 1. 运行 Lint 检查（必须通过，零错误）
npm run lint

# 2. 运行类型检查
npm run build

# 3. 检查是否有未使用的导入、变量、函数
# 4. 检查是否有 console.log（生产代码中应移除）
# 5. 检查是否有 TODO/FIXME 注释（应尽快处理或删除）
```

---

## 代码检查与 Lint

### ESLint 规则

- **零错误策略**：`--max-warnings 0`，不允许任何警告
- **未使用变量**：`@typescript-eslint/no-unused-vars` 必须修复
- **类型安全**：`@typescript-eslint/no-explicit-any` 尽量避免，如必须使用需添加注释说明
- **React Hooks**：必须遵循 React Hooks 规则

### 自动检查

**每次代码修改后，必须运行：**

```bash
npm run lint
```

**如果 Lint 失败，必须修复后才能继续开发。**

### 常见 Lint 错误及修复

1. **未使用的导入**
   ```typescript
   // ❌ 错误
   import React, { useState, useEffect } from 'react';
   // 如果只用了 useState，删除 useEffect
   
   // ✅ 正确
   import { useState } from 'react';
   ```

2. **未使用的变量**
   ```typescript
   // ❌ 错误
   const handleSkipHolding = async () => { ... };
   // 如果未使用，删除整个函数
   
   // ✅ 正确：删除未使用的函数
   ```

3. **React 导入**
   ```typescript
   // ❌ 错误（React 17+ 不需要）
   import React from 'react';
   
   // ✅ 正确
   import { useState, useEffect } from 'react';
   ```

---

## 代码整洁原则

### 1. 命名规范

- **组件**：PascalCase，描述性名称（`FundCard`, `PortfolioPage`）
- **函数/变量**：camelCase，动词开头（`handleClick`, `fetchData`）
- **常量**：UPPER_SNAKE_CASE（`MAX_RETRY_COUNT`）
- **类型/接口**：PascalCase，描述性（`FundDetail`, `RealtimeData`）

### 2. 函数设计

```typescript
// ✅ 好的函数：单一职责、清晰命名、类型安全
const calculateTotalProfit = (funds: Fund[]): number => {
  return funds.reduce((sum, fund) => sum + fund.profit, 0);
};

// ❌ 不好的函数：职责不清、命名模糊
const calc = (data: any) => {
  // 做了太多事情
};
```

### 3. 组件设计

- **组件职责单一**：一个组件只负责一个功能
- **Props 类型明确**：使用 TypeScript 接口定义 Props
- **避免过深嵌套**：超过 3 层嵌套应提取子组件
- **合理拆分**：超过 300 行的组件应考虑拆分

### 4. 代码组织

```
src/
├── components/     # UI 组件（按功能模块组织）
├── api/           # API 调用（按数据源组织）
├── store/         # 状态管理（Zustand stores）
├── db/            # 数据库 schema
├── constants/     # 常量定义
└── utils/         # 工具函数（如需要）
```

---

## 设计模式应用

### 1. 优先使用的设计模式

#### 单例模式（Singleton）
- **应用场景**：全局配置、数据库连接
- **示例**：Zustand stores、IndexedDB 实例

#### 观察者模式（Observer）
- **应用场景**：状态管理、事件系统
- **示例**：Zustand store subscriptions、React Context

#### 策略模式（Strategy）
- **应用场景**：多种算法可互换
- **示例**：不同的数据获取策略（JSONP、Fetch、WebSocket）

#### 工厂模式（Factory）
- **应用场景**：创建复杂对象
- **示例**：创建不同类型的 API 请求处理器

### 2. React 设计模式

#### 容器/展示组件分离
```typescript
// ✅ 展示组件（纯 UI）
function FundCard({ fund, onClick }: FundCardProps) {
  return <div onClick={onClick}>...</div>;
}

// ✅ 容器组件（逻辑处理）
function FundCardContainer({ fundCode }: { fundCode: string }) {
  const fund = useFundStore(state => state.getFund(fundCode));
  const handleClick = () => { /* ... */ };
  return <FundCard fund={fund} onClick={handleClick} />;
}
```

#### 自定义 Hooks
```typescript
// ✅ 提取可复用逻辑
function useFundData(code: string) {
  const [data, setData] = useState(null);
  useEffect(() => {
    fetchFundData(code).then(setData);
  }, [code]);
  return data;
}
```

#### 组合模式（Composition）
```typescript
// ✅ 使用组合而非继承
function Modal({ children, title }: ModalProps) {
  return (
    <div>
      <ModalHeader title={title} />
      <ModalBody>{children}</ModalBody>
    </div>
  );
}
```

---

## 架构设计原则

### 1. 分层架构

```
┌─────────────────────────────────┐
│   Presentation Layer (UI)      │  ← React Components
├─────────────────────────────────┤
│   Business Logic Layer          │  ← Zustand Stores, Hooks
├─────────────────────────────────┤
│   Data Access Layer             │  ← API Clients, IndexedDB
├─────────────────────────────────┤
│   External Services              │  ← Eastmoney API, etc.
└─────────────────────────────────┘
```

### 2. 依赖方向

- **单向数据流**：UI → Store → API
- **避免循环依赖**：模块间依赖关系清晰
- **依赖注入**：通过 Props/Context 传递依赖

### 3. 状态管理原则

- **本地状态**：使用 `useState`（组件内部状态）
- **共享状态**：使用 Zustand stores（跨组件状态）
- **服务器状态**：使用 Zustand + 缓存策略
- **URL 状态**：使用 React Router（路由相关）

### 4. 代码复用

- **提取公共逻辑**：创建自定义 Hooks
- **提取公共组件**：创建可复用 UI 组件
- **提取公共工具**：创建工具函数库
- **避免过度抽象**：只在真正需要时提取

---

## React/TypeScript 规范

### 1. TypeScript 类型

```typescript
// ✅ 明确的类型定义
interface FundData {
  code: string;
  name: string;
  nav: number;
}

// ✅ 使用类型推断（简单场景）
const count = 0; // 推断为 number

// ❌ 避免 any
function process(data: any) { ... } // 错误！

// ✅ 使用 unknown 或具体类型
function process(data: unknown) {
  if (isFundData(data)) {
    // 类型守卫后使用
  }
}
```

### 2. React Hooks 规范

```typescript
// ✅ 正确的依赖数组
useEffect(() => {
  fetchData(code);
}, [code]); // 包含所有依赖

// ✅ 清理副作用
useEffect(() => {
  const timer = setInterval(() => {}, 1000);
  return () => clearInterval(timer);
}, []);

// ❌ 避免在循环/条件中使用 Hooks
if (condition) {
  const [state, setState] = useState(); // 错误！
}
```

### 3. 组件 Props

```typescript
// ✅ 明确的 Props 类型
interface FundCardProps {
  fund: FundData;
  onClick?: (code: string) => void;
  className?: string;
}

// ✅ 使用默认值
function FundCard({ fund, onClick, className = '' }: FundCardProps) {
  // ...
}
```

### 4. 事件处理

```typescript
// ✅ 明确的类型
const handleClick = (e: React.MouseEvent<HTMLButtonElement>) => {
  e.preventDefault();
  // ...
};

// ✅ 使用 useCallback 优化（如需要）
const handleClick = useCallback((code: string) => {
  // ...
}, [dependencies]);
```

---

## API 与数据管理

### 1. API 调用规范

```typescript
// ✅ 统一的错误处理
async function fetchFundData(code: string): Promise<FundData> {
  try {
    const data = await api.get(`/fund/${code}`);
    return data;
  } catch (error) {
    console.error('获取基金数据失败:', error);
    throw error; // 向上抛出，由调用者处理
  }
}

// ✅ 请求队列管理（避免并发冲突）
const requestQueue = new Map<string, Promise<Data>>();
```

### 2. 数据缓存策略

- **实时数据**：短缓存（30秒-5分钟）
- **历史数据**：长缓存（1天）
- **静态数据**：永久缓存（IndexedDB）

### 3. 状态同步

- **乐观更新**：UI 立即更新，后台同步
- **错误回滚**：更新失败时恢复原状态
- **加载状态**：显示加载指示器

---

## 错误处理

### 1. 错误处理原则

```typescript
// ✅ 明确的错误类型
class FundNotFoundError extends Error {
  constructor(code: string) {
    super(`基金 ${code} 不存在`);
    this.name = 'FundNotFoundError';
  }
}

// ✅ 错误边界
function ErrorBoundary({ children }: { children: React.ReactNode }) {
  // ...
}

// ✅ 优雅降级
try {
  const data = await fetchData();
} catch (error) {
  // 显示友好错误信息
  showError('数据加载失败，请稍后重试');
  // 使用缓存数据
  const cachedData = getCachedData();
}
```

### 2. 错误日志

- **开发环境**：详细错误信息 + 堆栈
- **生产环境**：用户友好提示 + 错误上报（如需要）

---

## 性能优化

### 1. React 性能优化

```typescript
// ✅ 使用 React.memo（纯组件）
const FundCard = React.memo(({ fund }: FundCardProps) => {
  // ...
});

// ✅ 使用 useMemo（昂贵计算）
const total = useMemo(() => {
  return funds.reduce((sum, f) => sum + f.value, 0);
}, [funds]);

// ✅ 使用 useCallback（函数引用稳定）
const handleClick = useCallback(() => {
  // ...
}, [dependencies]);
```

### 2. 代码分割

```typescript
// ✅ 路由级别代码分割
const PortfolioPage = lazy(() => import('./PortfolioPage'));

// ✅ 组件级别代码分割（大型组件）
const HeavyChart = lazy(() => import('./HeavyChart'));
```

### 3. 资源优化

- **图片**：使用 WebP 格式，懒加载
- **字体**：使用 `font-display: swap`
- **Bundle**：代码分割，Tree Shaking

---

## 代码审查检查点

在代码审查时，检查以下要点：

- [ ] 所有 Lint 错误已修复
- [ ] 没有未使用的导入、变量、函数
- [ ] TypeScript 类型完整，没有 `any`
- [ ] 函数职责单一，命名清晰
- [ ] 组件大小合理（< 300 行）
- [ ] 错误处理完善
- [ ] 性能优化已考虑（memo、useMemo、useCallback）
- [ ] 代码注释清晰（解释"为什么"）
- [ ] 没有硬编码的魔法数字/字符串
- [ ] 遵循项目架构和设计模式

---

## 违反规范的后果

- **Lint 错误**：代码无法合并，必须修复
- **类型错误**：构建失败，必须修复
- **未使用代码**：必须删除
- **架构违规**：代码审查时指出，需要重构

---

## 持续改进

- 定期审查和更新本规范
- 根据项目发展调整最佳实践
- 团队讨论并达成共识

---

**记住：代码是写给人看的，机器只是顺便执行。**
